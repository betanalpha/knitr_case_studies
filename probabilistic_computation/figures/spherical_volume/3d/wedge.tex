\documentclass{standalone}
\usepackage{graphicx}	
\usepackage{amssymb, amsmath, amsthm}
\usepackage{color}

\usepackage{tikz}
\usetikzlibrary{intersections, backgrounds}

\definecolor{light}{RGB}{220, 188, 188}
\definecolor{mid}{RGB}{185, 124, 124}
\definecolor{dark}{RGB}{143, 39, 39}
\definecolor{highlight}{RGB}{180, 31, 180}
\definecolor{gray10}{gray}{0.1}
\definecolor{gray20}{gray}{0.2}
\definecolor{gray30}{gray}{0.3}
\definecolor{gray40}{gray}{0.4}
\definecolor{gray60}{gray}{0.6}
\definecolor{gray70}{gray}{0.7}
\definecolor{gray80}{gray}{0.8}
\definecolor{gray90}{gray}{0.9}
\definecolor{gray95}{gray}{0.95}
\newcommand{\dd}{ \mathrm{d} }

\begin{document}

\begin{tikzpicture}[scale=0.35, thick]
  \draw[white] (-12, -12) rectangle (12, 12);
  
  \pgfmathsetmacro{\thetaa}{20}
  \pgfmathsetmacro{\thetab}{50}
  \pgfmathsetmacro{\phia}{45}
  \pgfmathsetmacro{\phib}{75}
  
  \fill[color=dark] (0, 0) circle (5pt);  
  \fill[color=light] (0, 0) circle (3pt);
  
  \draw[dashed, color=gray60] (8, 0) arc (0:360:8);
 
  \draw [dashed, color=gray60, domain={\thetaa}:{\thetab}, samples=30] 
    plot ({8 * cos(\x) * cos(\phia)}, {8 * sin(\x)});

  \draw [dashed, color=gray60, domain={\thetaa}:{\thetab}, samples=30] 
    plot ({8 * cos(\x) * cos(\phib)}, {8 * sin(\x)});

  \draw [dashed, color=gray60, domain={\phia}:{\phib}, samples=30] 
    plot ({8 * cos(\thetaa) * cos(\x)}, {8 * sin(\thetaa)});

  \draw [dashed, color=gray60, domain={\phia}:{\phib}, samples=30] 
    plot ({8 * cos(\thetab) * cos(\x)}, {8 * sin(\thetab)});

  \draw    ({5 * cos(\thetaa) * cos(\phia)}, {5 * sin(\thetaa)}) 
        -- ({11 * cos(\thetaa) * cos(\phia)}, {11 * sin(\thetaa)});
  \draw    ({5 * cos(\thetaa) * cos(\phib)}, {5 * sin(\thetaa)}) 
        -- ({11 * cos(\thetaa) * cos(\phib)}, {11 * sin(\thetaa)});

  \draw [domain={\thetaa}:{\thetab}, samples=30] 
    plot ({5 * cos(\x) * cos(\phia)}, {5 * sin(\x)});

  \draw [domain={\thetaa}:{\thetab}, samples=30] 
    plot ({11 * cos(\x) * cos(\phia)}, {11 * sin(\x)});

  \draw [domain={\thetaa}:{\thetab}, samples=30] 
    plot ({5 * cos(\x) * cos(\phib)}, {5 * sin(\x)});

  \draw [domain={\thetaa}:{\thetab}, samples=30] 
    plot ({11 * cos(\x) * cos(\phib)}, {11 * sin(\x)});

  \draw [domain={\phia}:{\phib}, samples=30] 
    plot ({5 * cos(\thetaa) * cos(\x)}, {5 * sin(\thetaa)});

  \draw [domain={\phia}:{\phib}, samples=30] 
    plot ({11 * cos(\thetaa) * cos(\x)}, {11 * sin(\thetaa)});

  \draw [domain={\phia}:{\phib}, samples=30] 
    plot ({5 * cos(\thetab) * cos(\x)}, {5 * sin(\thetab)});

  \draw [domain={\phia}:{\phib}, samples=30] 
    plot ({11 * cos(\thetab) * cos(\x)}, {11 * sin(\thetab)});

  \draw    ({5 * cos(\thetab) * cos(\phia)}, {5 * sin(\thetab)})
        -- ({11 * cos(\thetab) * cos(\phia)}, {11 * sin(\thetab)});
  \draw    ({5 * cos(\thetab) * cos(\phib)}, {5 * sin(\thetab)}) 
        -- ({11 * cos(\thetab) * cos(\phib)}, {11 * sin(\thetab)});
        
  \draw[|<->|, >=stealth, light]     ({5 * cos(\thetaa - 5) * cos(\phia)}, {5 * sin(\thetaa - 5)}) 
                                  -- ({7.9 * cos(\thetaa - 5) * cos(\phia)}, {7.9 * sin(\thetaa - 5)});
                                  
  \node[] at ({6.5 * cos(\thetaa - 10) * cos(\phia)}, {6.5 * sin(\thetaa - 10)}) { $\delta$ };

  \draw[|<->|, >=stealth, light]     ({8.1 * cos(\thetaa - 5) * cos(\phia)}, {8.1 * sin(\thetaa - 5)}) 
                                  -- ({11 * cos(\thetaa - 5) * cos(\phia)}, {11 * sin(\thetaa - 5)});

  \node[] at ({9.5 * cos(\thetaa - 10) * cos(\phia)}, {9.5 * sin(\thetaa - 10)}) { $\delta$ };

\end{tikzpicture}

\end{document}  