\documentclass{standalone}
\usepackage{graphicx}	
\usepackage{amssymb, amsmath, amsthm}
\usepackage{color}

\usepackage{tikz}
\usetikzlibrary{arrows.meta}

\definecolor{light}{RGB}{220, 188, 188}
\definecolor{mid}{RGB}{185, 124, 124}
\definecolor{dark}{RGB}{143, 39, 39}
\definecolor{highlight}{RGB}{180, 31, 180}
\definecolor{gray10}{gray}{0.1}
\definecolor{gray20}{gray}{0.2}
\definecolor{gray30}{gray}{0.3}
\definecolor{gray40}{gray}{0.4}
\definecolor{gray60}{gray}{0.6}
\definecolor{gray70}{gray}{0.7}
\definecolor{gray80}{gray}{0.8}
\definecolor{gray90}{gray}{0.9}
\definecolor{gray60}{gray}{0.95}

\begin{document}

\begin{tikzpicture}[scale=0.25, thick]
  \draw[white] (-10, -27) rectangle (36, 8);
 
  \node[rotate=90] at (-8, 0) { Variable-By-Variable };
  \node[rotate=90] at (-8, -18) { Module-By-Module };
 
  % Top Row
  \begin{scope}[shift={(0, 0)}]
    \foreach \n in {33, 35, 16, 21, 19} {
      \pgfmathsetmacro{\r}{mod(\n - 1, 5) + 1}
      \pgfmathsetmacro{\theta}{45 * floor((\n - 1) / 5)}
      \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
  \end{scope}
  
  \begin{scope}[shift={(14, 0)}]
    \foreach \n in {24, 2, 16, 33, 8, 11, 27, 15, 31, 4, 5, 9, 32, 37, 3, 20, 23, 17, 26, 12} {
      \pgfmathsetmacro{\r}{mod(\n - 1, 5) + 1}
      \pgfmathsetmacro{\theta}{45 * floor((\n - 1) / 5)}
      \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
  \end{scope}
  
  \begin{scope}[shift={(28, 0)}]
    \foreach \r in {1, 2, 3, 4, 5} {
      \foreach \theta in {0, 45, ..., 315} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
    }
  \end{scope}

  % Bottom Row
  \begin{scope}[shift={(0, -18)}]
    \begin{scope}[shift={(1, 1)}]
    \foreach \r/\theta in {1/0, 2/45, 4/0, 4/45, 5/0} {
      \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
    \end{scope}
    
    \begin{scope}[shift={(-1, 1)}]
    \foreach \r/\theta in {1/90, 1/135, 2/90, 3/135, 4/135} {
      \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
    \end{scope}
    
    \begin{scope}[shift={(-1, -1)}]
    \foreach \r/\theta in {1/225, 2/180, 3/180, 4/225, 5/180} {
       \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
    \end{scope}
    
    \begin{scope}[shift={(1, -1)}]
    \foreach \r/\theta in {2/270, 4/270, 5/270, 5/315} {
      \filldraw[fill=mid, draw=dark] 
                 ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                 ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                 ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                 ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                 ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                 cycle;
    }
    \end{scope}
    
    \pgfmathsetmacro{\delta}{0.055}
    \fill[white] (-1 + \delta, -2) rectangle (1 - \delta, 2);
    \fill[white] (-2, -1 + \delta) rectangle (2, 1 - \delta);
    
  \end{scope}
  
  \begin{scope}[shift={(14, -18)}]
    \foreach \r in {1, 2, 3, 4, 5} {
    
      \begin{scope}[shift={(1, 1)}]
      \foreach \theta in {0, 45} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
      \end{scope}
      
      \begin{scope}[shift={(-1, 1)}]
      \foreach \theta in {90, 135} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
      \end{scope}
      
      \begin{scope}[shift={(-1, -1)}]
      \foreach \theta in {180, 225} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
      \end{scope}
      
      \begin{scope}[shift={(1, -1)}]
      \foreach \theta in {270, 315} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
      \end{scope}
    }
    
    \pgfmathsetmacro{\delta}{0.055}
    \fill[white] (-1 + \delta, -2) rectangle (1 - \delta, 2);
    \fill[white] (-2, -1 + \delta) rectangle (2, 1 - \delta);
    
  \end{scope}
  
  \begin{scope}[shift={(28, -18)}]
    \foreach \r in {1, 2, 3, 4, 5} {
      \foreach \theta in {0, 45, ..., 315} {
        \filldraw[fill=mid, draw=dark]
                   ({(\r - 1) * cos(\theta)}, {(\r - 1) * sin(\theta)}) -- 
                   ({\r * cos(\theta)}, {\r * sin(\theta)}) --
                   ({(\r * cos(\theta)}, {(\r * sin(\theta)}) arc (\theta:\theta + 45:\r) --
                   ({\r * cos(\theta + 45)}, {\r * sin(\theta + 45)}) --
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) -- 
                   ({(\r - 1) * cos(\theta + 45)}, {(\r - 1) * sin(\theta + 45)}) arc (\theta + 45:\theta:\r - 1) --
                   cycle;
      }
    }
  \end{scope}
  
\end{tikzpicture}

\end{document}  